// Created on 07-Mar-24
//
#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vl;

int main() {
    int n, b;
    cin >> n >> b;
    vi v(n);
    for (auto &x: v) {
        cin >> x;
    }
    int cuts = 0, bLeft = b;
    for (int i = 0; i < n - 1; i++) {
        if (v[i] % 2 == 0 && v)
    }

    return 0;
}